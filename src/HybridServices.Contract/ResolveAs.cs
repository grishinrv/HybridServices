namespace HybridServices.Contract
{
    /// <summary>
    /// Indicates, if implementation should be considered to be local or remote.
    /// </summary>
    public enum ResolveAs
    {
        Local = 0,
        Remote = 1
    }
}